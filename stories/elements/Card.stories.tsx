import React from 'react'

import { Card } from '../..'

export default {
  title: 'Elements/Card',
  component: Card,
}

export const _Card = (props: any) => <Card {...props}>This is just a card.</Card>
